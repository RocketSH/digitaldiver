import type { NextPage } from "next";
import Layout from "@layout";
import Hero from "../layout/Hero";

const Home = () => {
  return (
    <Layout>
      <Hero />
    </Layout>
  );
};

export default Home;
